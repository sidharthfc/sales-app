"""
route_sales.api
===============
Whitelisted REST endpoints for the Route Sales mobile/web clients.

Base URL
--------
  /api/method/route_sales.api.<module>.<function>

Modules
-------
  (added incrementally as APIs are defined)

Auth
----
All endpoints require a valid Frappe session or API key unless
explicitly decorated with allow_guest=True.
"""
